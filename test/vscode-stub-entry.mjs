const stub = globalThis.__vscodeStub;
export const {
	l10n, Uri, ViewColumn, EventEmitter, Disposable, env, workspace, commands, window,
	extensions, ExternalUriOpenerPriority, UIKind,
} = stub;
export default stub;
